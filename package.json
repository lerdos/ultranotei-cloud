{
  "name": "wallet.conceal.network",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^1.2.12",
    "@fortawesome/free-solid-svg-icons": "^5.6.3",
    "@fortawesome/react-fontawesome": "^0.1.3",
    "bootstrap": "^4.2.1",
    "immutability-helper": "^2.8.1",
    "jwt-decode": "^2.2.0",
    "qrcode.react": "^0.9.2",
    "react": "^16.6.3",
    "react-bootstrap": "^1.0.0-beta.3",
    "react-dom": "^16.6.3",
    "react-ionicons": "^3.0.1",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "",
    "aws:dev": "rm -rf ./build && react-scripts build && aws s3 cp --recursive ./build/ s3://ccx-webwallet-dev-v0.1.0/ --profile=priv --cache-control max-age=30 --region=us-west-2 --metadata-directive REPLACE"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
